import { equal as == } from builtins;

// A bare `none` passed to an `any?` parameter pins the type variable to the
// `NoneLiteral` pseudo-type and monomorphizes the function for the none-only
// case. `match` over the param always takes the `none` arm.
fn is_present(x: any?) -> bool {
    match (x) {
        some(v) => true,
        none => false,
    }
}.test({
    assert(is_present(none) == false);
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 26
binary size: 1008
  code: 144
  data: 85
  metadata: 779
functions:
  aipl_assert:
    calls: 1
    bytes: 0
  aipl_test_begin:
    calls: 1
    bytes: 0
  aipl_test_end:
    calls: 1
    bytes: 0
  aipl_test_summary:
    calls: 1
    bytes: 0
  main:
    calls: 1
    bytes: 144
