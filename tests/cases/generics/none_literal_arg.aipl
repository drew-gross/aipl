import { == } from builtins;

// A bare `none` passed to an `any?` parameter pins the type variable to the
// `NoneLiteral` pseudo-type and monomorphizes the function for the none-only
// case. `match` over the param always takes the `none` arm.
fn is_present(x: any?) -> bool {
    match (x) {
        some(v) => true,
        none => false,
    }
}.test({
    assert(is_present(none) == false);
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 31
binary size: 1136
  code: 180
    __test$is_present: 80
    is_present$NoneLiteral: 28
    main: 72
  data: 85
  metadata: 871
builtin calls: 4
  aipl_assert: 1
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
