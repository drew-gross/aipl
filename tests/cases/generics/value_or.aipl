import { equal as == } from builtins;

// A named type parameter `<T: any>` ties several occurrences of `T` to the
// same concrete type and lets `T` appear in the return type. `value_or`
// monomorphizes per `T`; the type can be pinned by either argument.
fn value_or<T: any>(self: T?, default: T) -> T {
    match (self) {
        some(v) => v, #[allow]
        none => default,
    }
}.test({
    assert(value_or(some(7), 0) == 7); // i64 -> 7
    assert(value_or(none, 35) == 35); // i64 (pinned by default) -> 35
    assert(value_or(some('a'), 'z') == 'a'); // char -> 'a'
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 70
binary size: 1464
  code: 292
  data: 203
  metadata: 969
functions:
  aipl_assert:
    calls: 3
    bytes: 0
  aipl_test_begin:
    calls: 1
    bytes: 0
  aipl_test_end:
    calls: 1
    bytes: 0
  aipl_test_summary:
    calls: 1
    bytes: 0
  main:
    calls: 1
    bytes: 268
  value_or$i64:
    calls: 2
    bytes: 24
