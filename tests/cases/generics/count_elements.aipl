import { push, wrapping_increment as ++, == } from builtins;

// A generic that iterates the array elements (binding each as the concrete
// element type per instance) and counts them. Exercises element-level body
// operations across i64[], char[], and bool[] instances.

fn count(v: any[]) -> i64 {
    mut n = 0;
    for (let x : v) {
        set n++;
    }
    n
}.test({
    assert(count([10, 20, 30, 40]) == 4);
    assert(count(['x', 'y']) == 2);
    mut bs = [true];
    set bs.push(false);
    assert(count(bs) == 2);
})
--- performance ---
allocations: 4
deallocations: 4
reallocations: 0
bytes allocated: 149
instructions executed: 457
binary size: 2576
  code: 984
    main: 984
  data: 154
  metadata: 1438
builtin calls: 23
  aipl_arr_inc: 2
  aipl_array_dec: 5
  aipl_array_new: 2
  aipl_array_push: 1
  aipl_assert: 3
  aipl_dec: 1
  aipl_str_alloc: 1
  aipl_str_data: 1
  aipl_str_iter_init: 1
  aipl_str_iter_next: 1
  aipl_str_len: 2
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
