// The `++` mirror of `err_bare_plus_operator`: `++` is recognised as an operator
// spelling and refused the same way — it has no bare form either, incrementing
// being an add with the same wrapping/saturating choice to make.
import { ++ } from builtins;

fn main() {}
--- errors ---
error: the "++" operator has no bare form; pick a semantics and import it aliased, e.g. `wrapping_increment as ++` or `saturating_increment as ++` from builtins
 --> ./tests/cases/imports/err_bare_incr_operator.aipl:4:10
  |
4 | import { ++ } from builtins;
  |          ^^
