// The `--` mirror of `err_bare_incr_operator`: `--` is recognised as an operator
// spelling and refused the same way — it has no bare form either, decrementing
// being a subtract with the same wrapping/saturating choice to make.
import { -- } from builtins;

fn main() {}
--- errors ---
error: an operator has no bare import: import the builtin it stands for and alias it to the operator, e.g. `wrapping_add as +`
 --> ./tests/cases/imports/err_bare_decr_operator.aipl:4:10
  |
4 | import { -- } from builtins;
  |          ^^
