import { equal as ==, less_than as <, wrapping_add as +, wrapping_increment as ++ } from builtins;

// `while (cond) { .. }` runs the body while `cond` (a bool) holds, re-checking
// it before each pass. The counter is a `mut` declared before the loop; the
// body reassigns it with `set`, and the change persists across iterations.
fn sum_below(n: i64) -> i64 {
    mut i = 0;
    mut total = 0;
    while (i < n) {
        set total = total + i;
        set i++;
    }
    total
}.test({
    assert(sum_below(5) == 10); // 0+1+2+3+4
    assert(sum_below(1) == 0);
    assert(sum_below(0) == 0); // condition false up front: body never runs
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 208
binary size: 1440
  code: 292
  data: 173
  metadata: 975
functions:
  aipl_assert:
    calls: 3
    bytes: 0
  aipl_test_begin:
    calls: 1
    bytes: 0
  aipl_test_end:
    calls: 1
    bytes: 0
  aipl_test_summary:
    calls: 1
    bytes: 0
  main:
    calls: 1
    bytes: 200
  sum_below:
    calls: 3
    bytes: 92
