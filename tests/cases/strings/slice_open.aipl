import { equal as == } from builtins;

// `s[start..]` slices from `start` to the end of the string (the open end runs
// to `s`'s length, filled in by codegen — no `len` import needed). `start` is
// clamped to `[0, len]`, like the closed `s[start..end]` form.
fn drop_prefix(s: str, n: i64) -> str { s[n..] }.test({
    // The whole-range form is what `slice_whole` flags — squelched here, since
    // exercising it *is* the point of these two lines.
    assert("hello"[0..] == "hello"); #[allow]
    // whole string
    assert("hello"[2..] == "llo"); // from the 3rd byte
    assert("hello"[5..] == ""); // start == len -> empty
    assert("hello"[10..] == ""); // start past end -> clamped to empty
    assert(drop_prefix("abcdef", 4) == "ef"); // 5th byte onward
    assert(""[0..] == ""); #[allow]

    // empty source
    // The closed form still works (regression).
    assert("hello"[1..3] == "el");
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 101
binary size: 3360
  code: 1076
  data: 385
  metadata: 1899
functions:
  aipl_assert:
    calls: 7
    bytes: (external)
  aipl_dec:
    calls: 21
    bytes: (external)
  aipl_inc:
    calls: 7
    bytes: (external)
  aipl_str_eq:
    calls: 7
    bytes: (external)
  aipl_str_len:
    calls: 6
    bytes: (external)
  aipl_str_slice:
    calls: 7
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  main:
    calls: 1
    bytes: 1076
