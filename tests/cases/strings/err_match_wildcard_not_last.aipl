import { ==, print } from builtins;

// The `_` arm must be last — arms after it can never run.
fn label(s: str) -> str { match (s) { _ => "default", "a" => "x" } }

fn main() !prints {
    print(label("a"));
}
--- errors ---
error: the `_` arm must be last (arms after it are unreachable)
 --> ./tests/cases/strings/err_match_wildcard_not_last.aipl:4:39
  |
4 | fn label(s: str) -> str { match (s) { _ => "default", "a" => "x" } }
  |                                       ^
