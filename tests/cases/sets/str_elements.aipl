import { concat as +++, equal as ==, has, len, logical_not as !, not_equal as != } from builtins;

// Sets of strings: deduplicated and tested for membership by *content* (not
// pointer identity), and freed/retained like a `str[]`.
fn distinct(xs: #{str}) -> u64 { len(xs) }.test({
    assert(distinct(#{"apple", "banana", "apple", "cherry"}) == 3);
})

fn build() -> #{str} {
    #{"apple", "banana", "apple", "cherry"} // -> {"apple", "banana", "cherry"}
}.test({
    let s = build();
    assert(s == #{"apple", "banana", "cherry"});
    assert(len(s) == 3);
    assert(s.has("banana"));
    assert(!s.has("grape"));

    // Membership is by content: a freshly built "banana" still matches.
    let key = "ba" +++ "nana";
    assert(s.has(key));

    // Equality is order-independent and by content.
    assert(#{"a", "b"} == #{"b", "a"});
    assert(#{"a", "b"} != #{"a", "c"});
})
--- performance ---
allocations: 8
deallocations: 8
reallocations: 0
bytes allocated: 416
instructions executed: 546
binary size: 5216
  code: 2220
  data: 453
  metadata: 2543
functions:
  __eq_set$str:
    calls: 3
    bytes: 256
  aipl_arr_drop_str:
    calls: 7
    bytes: 0
  aipl_arr_retain_ptr:
    calls: 17
    bytes: 0
  aipl_array_dec:
    calls: 7
    bytes: 0
  aipl_array_push_mut:
    calls: 17
    bytes: 0
  aipl_array_with_cap:
    calls: 7
    bytes: 0
  aipl_assert:
    calls: 8
    bytes: 0
  aipl_concat_lazy:
    calls: 1
    bytes: 0
  aipl_dec:
    calls: 20
    bytes: 0
  aipl_inc:
    calls: 17
    bytes: 0
  aipl_set_contains:
    calls: 29
    bytes: 0
  aipl_set_insert:
    calls: 19
    bytes: 0
  aipl_str_len:
    calls: 52
    bytes: 0
  aipl_test_begin:
    calls: 2
    bytes: 0
  aipl_test_end:
    calls: 2
    bytes: 0
  aipl_test_summary:
    calls: 1
    bytes: 0
  main:
    calls: 1
    bytes: 1964
