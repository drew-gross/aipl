import { < } from builtins;
// A comparison yields `bool`, which doesn't match the declared `i64` return.
fn f() -> i64 { 1 < 2 }
fn main() -> i64 { 0 }
--- errors ---
error: fn "f": body returns bool, but the declared return type is i64
 --> input:3:17
  |
3 | fn f() -> i64 { 1 < 2 }
  |                 ^^^^^
