// A bare `i64` is not an `i64?` — an optional must be built with `some`/`none`.
fn f() -> i64? { 5 }
fn main() -> i64 { 0 }
--- errors ---
error: fn "f": body returns i64, but the declared return type is i64?
 --> input:2:18
  |
2 | fn f() -> i64? { 5 }
  |                  ^
