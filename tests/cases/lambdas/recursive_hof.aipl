import {
    equal as ==,
    less_than_or_equal as <=,
    wrapping_add as +,
    wrapping_sub as -,
} from builtins;

// `rec` forwards its function parameter to itself recursively, then calls it at
// the base case. Specialization is memoized by (callee, synthesized function),
// so the recursive forward reuses `rec`'s specialization instead of spawning a
// new one forever.
fn rec(n: i64, f: (i64) -> i64) -> i64 { if (n <= 0) { f(0) } else { rec(n - 1, f) } }.test({
    let bump = 7;
    assert(rec(3, |x| x + bump) == 7); // f(0) = 0 + 7 = 7
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 51
binary size: 1112
  code: 192
  data: 54
  metadata: 866
functions:
  __test$rec:
    calls: 1
    bytes: 64
  aipl_assert:
    calls: 1
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  main:
    calls: 1
    bytes: 68
  rec$lam1:
    calls: 0
    bytes: 32
  rec$lam1$tail:
    calls: 4
    bytes: 28
