import { equal as ==, map, to_str, wrapping_add as +, wrapping_mul as * } from builtins;

// `map` is a builtin: no user definition needed. It desugars to an inline loop
// in the enclosing scope, so lambdas (including capturing ones) just work.
fn doubled(xs: i64[]) -> i64[] { xs.map(|x| x * 2) }.test({
    let xs = [1, 2, 3];
    assert(xs.map(|x| x * 2) == [2, 4, 6]); // method form
    assert(map(xs, |x| x + 1) == [2, 3, 4]); // function form

    // A captured variable.
    let k = 10;
    assert(xs.map(|x| x + k) == [11, 12, 13]);

    // map may change the element type.
    assert(xs.map(to_str) == ["1", "2", "3"]);

    // chained / nested.
    assert(xs.map(|x| x + 1).map(|y| y * y) == [4, 9, 16]);
})
--- performance ---
allocations: 14
deallocations: 14
reallocations: 2
bytes allocated: 715
instructions executed: 2136
binary size: 6416
  code: 3488
  data: 312
  metadata: 2616
functions:
  __eq_i64$arr:
    calls: 4
    bytes: 296
  __eq_str$arr:
    calls: 1
    bytes: 364
  __map10$own0:
    calls: 1
    bytes: 252
  __test$doubled:
    calls: 1
    bytes: 2264
  __to_str_i64:
    calls: 3
    bytes: 204
  aipl_arr_inc:
    calls: 2
    bytes: (external)
  aipl_array_dec:
    calls: 13
    bytes: (external)
  aipl_array_new:
    calls: 6
    bytes: (external)
  aipl_array_push_mut:
    calls: 15
    bytes: (external)
  aipl_array_with_cap:
    calls: 5
    bytes: (external)
  aipl_assert:
    calls: 5
    bytes: (external)
  aipl_i64_len:
    calls: 6
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  aipl_write_i64:
    calls: 3
    bytes: (external)
  main:
    calls: 1
    bytes: 108
