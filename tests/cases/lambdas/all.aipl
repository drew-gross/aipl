import {
    all,
    equal as ==,
    filter,
    greater_than as >,
    less_than_or_equal as <=,
    logical_not as !,
} from builtins;

// `arr.all(pred)` is true when every element satisfies `pred` (short-circuits at
// the first that fails; an empty array is vacuously true). A higher-order
// builtin like `filter`/`map`: the lambda is lifted, captures threaded through.
// Unlike those, `all` is implemented in AIPL itself (`builtin_all.aipl`) and
// specializes like a user generic.
fn is_pos(self: i64) -> bool { self > 0 }

fn positive(xs: i64[]) -> bool { xs.all(|x| x > 0) }.test({
    assert([2, 4, 6].all(|x| x <= 6)); // every element satisfies it
    assert(![1, 2, 9].all(|x| x <= 5)); // 9 fails -> false
    assert(all([5, 5], |x| x == 5)); // free-call form
    assert([1, 2].all(is_pos)); // a named predicate, no lambda
    assert(['a', 'a', 'a'].all(|c| c == 'a')); // any comparable element type
    // Vacuously true on an empty array (a typed empty from `filter`).
    assert([1, 2].filter(|x| x > 100).all(|x| x <= 0));
    // Method-chain helper + a captured bound.
    assert(positive([1, 2, 3]));
    assert(!positive([1, 0, 3]));
})
--- performance ---
allocations: 1
deallocations: 1
reallocations: 0
bytes allocated: 48
instructions executed: 1092
binary size: 6072
  code: 2656
  data: 767
  metadata: 2649
functions:
  __builtin_all$char$lam$__test$positive$lambda3:
    calls: 1
    bytes: 244
  __builtin_all$i64$lam$__test$positive$lambda0:
    calls: 1
    bytes: 204
  __builtin_all$i64$lam$__test$positive$lambda1:
    calls: 1
    bytes: 204
  __builtin_all$i64$lam$__test$positive$lambda2:
    calls: 1
    bytes: 204
  __builtin_all$i64$lam$__test$positive$lambda6:
    calls: 1
    bytes: 204
  __builtin_all$i64$lam$__test$positive$lambda7:
    calls: 1
    bytes: 204
  __builtin_all$i64$lam$__test$positive$lambda8:
    calls: 1
    bytes: 204
  __builtin_all$i64$lam$is_pos:
    calls: 1
    bytes: 204
  __test$positive:
    calls: 1
    bytes: 568
  __test$positive$filter5$own0:
    calls: 1
    bytes: 308
  aipl_arr_inc:
    calls: 1
    bytes: (external)
  aipl_arr_reserve:
    calls: 1
    bytes: (external)
  aipl_array_dec:
    calls: 3
    bytes: (external)
  aipl_assert:
    calls: 8
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  main:
    calls: 1
    bytes: 108
