// A result payload (either side) must be a scalar, `str`, a struct, or an
// array — variant payloads aren't supported yet.
variant V = A | B

fn bad() -> V!str { ok(A) }

fn main() {}
--- errors ---
error: fn "bad": a result Ok payload must be i64, bool, char, str, a struct, an array, or unit ("!E"), got V
