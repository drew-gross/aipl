// A result payload (either side) must be a scalar, `str`, a struct, a
// variant, or an array — optional payloads aren't supported yet.
fn bad() -> i64?!str { ok(some(1)) }

fn main() {}
--- errors ---
error: fn "bad": a result Ok payload must be i64, bool, char, str, a struct, a variant, an array, or unit ("!E"), got i64?
