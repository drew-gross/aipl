import { concat as +++, divide as /, equal as ==, print, to_str } from builtins;

// A result `T!E` is `ok(v)` or `err(e)` — an inline `{tag, value}` (tag 1 = ok,
// 0 = err), matched like an optional. `ok`/`err` are constructors like
// `some`/`none`: the unbound side is inferred from context.
fn safe_div(a: i64, b: i64) -> i64!str { if (b == 0) { err("divide by zero") } else { ok(a / b) } }

fn describe(a: i64, b: i64) -> str {
    match (safe_div(a, b)) {
        ok(v) => "= " +++ to_str(v),
        err(e) => "error: " +++ e,
    }
}.test({
    assert(describe(10, 2) == "= 5");
    assert(describe(1, 0) == "error: divide by zero");

    // Match on a result value directly.
    let r = safe_div(9, 3);
    match (r) {
        ok(v) => assert(v == 3),
        err(e) => assert(false),
    };
})

fn main() !prints {
    print(describe(10, 2));
    print(describe(1, 0));
}
--- stdout ---
= 5
error: divide by zero
--- performance ---
allocations: 2
deallocations: 2
reallocations: 0
bytes allocated: 80
instructions executed: 134
binary size: 2072
  code: 824
  data: 32
  metadata: 1216
functions:
  __to_str_i64:
    calls: 1
    bytes: 180
  aipl_concat_lazy:
    calls: 2
    bytes: (external)
  aipl_dec:
    calls: 14
    bytes: (external)
  aipl_i64_len:
    calls: 2
    bytes: (external)
  aipl_inc:
    calls: 9
    bytes: (external)
  aipl_print:
    calls: 2
    bytes: (external)
  aipl_str_len:
    calls: 4
    bytes: (external)
  aipl_write_i64:
    calls: 1
    bytes: (external)
  describe:
    calls: 2
    bytes: 480
  main:
    calls: 1
    bytes: 164
