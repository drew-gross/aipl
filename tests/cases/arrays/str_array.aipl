import { equal as ==, len, push, wrapping_increment as ++ } from builtins;

// A `str[]` is an array whose elements are heap-allocated strings. Build,
// index (yields `str?`), push, and iterate — each operation retains/releases
// the element strings so nothing leaks or double-frees. `ys` snapshots `xs`
// before pushing, so `xs` stays length 2 (value semantics).
fn appended(xs: str[], s: str) -> str[] {
    mut ys = xs;
    set ys.push(s);
    ys
}.test({
    let xs = ["foo", "bar"];
    mut ys = xs;
    set ys.push("baz");
    // index yields str?
    assert(ys[0] == some("foo"));
    assert(ys == ["foo", "bar", "baz"]);
    mut count = 0;
    for (let s : ys) {
        set count++;
    }
    assert(count == 3);
    // xs is unchanged (len 2)
    assert(xs == ["foo", "bar"]);
    assert(len(xs) == 2);
    assert(len(ys) == 3);
    assert(appended(xs, "baz") == ["foo", "bar", "baz"]);
})
--- performance ---
allocations: 6
deallocations: 6
reallocations: 0
bytes allocated: 320
instructions executed: 854
binary size: 4472
  code: 1864
  data: 375
  metadata: 2233
functions:
  __eq_str$arr:
    calls: 3
    bytes: 308
  __eq_str$opt:
    calls: 1
    bytes: 100
  __test$appended:
    calls: 1
    bytes: 1384
  aipl_arr_drop_str:
    calls: 6
    bytes: (external)
  aipl_arr_inc:
    calls: 4
    bytes: (external)
  aipl_arr_retain_ptr:
    calls: 4
    bytes: (external)
  aipl_array_dec:
    calls: 10
    bytes: (external)
  aipl_array_new:
    calls: 4
    bytes: (external)
  aipl_array_push:
    calls: 2
    bytes: (external)
  aipl_assert:
    calls: 7
    bytes: (external)
  aipl_dec:
    calls: 18
    bytes: (external)
  aipl_inc:
    calls: 8
    bytes: (external)
  aipl_str_eq:
    calls: 9
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  main:
    calls: 1
    bytes: 72
