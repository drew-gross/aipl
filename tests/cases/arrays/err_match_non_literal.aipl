import { wrapping_add as +, print, to_str } from builtins;

// Array-pattern elements must be literals, not computed expressions.
fn code(xs: i64[]) -> i64 {
    let k = 2;
    match (xs) { [1, k + 1] => 1, _ => 0 }
}

fn main() !prints {
    print(to_str(code([1, 2])));
}
--- errors ---
error: array-pattern elements must be literals
 --> ./tests/cases/arrays/err_match_non_literal.aipl:6:22
  |
6 |     match (xs) { [1, k + 1] => 1, _ => 0 }
  |                      ^^^^^
