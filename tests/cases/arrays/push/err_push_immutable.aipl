import { push } from builtins;

// `push` needs a mutable receiver; a `let` binding can't be pushed to.
fn main() -> i64 {
    let xs = [1];
    set xs.push(2);
    0
}
--- errors ---
error: set: cannot assign to immutable binding "xs" (use "let mut")
 --> ./tests/cases/arrays/push/err_push_immutable.aipl:6:9
  |
6 |     set xs.push(2);
  |         ^^^^^^^^^
