import { push, == } from builtins;

// Growable arrays: start from an empty literal and push. `xs.push(x)` grows
// the array variable in place. Snapshotting it first (`let b = a`) keeps `b`
// independent — pushing to `a` afterward doesn't change `b` (value semantics).
fn appended(xs: i64[], x: i64) -> i64[] {
    mut ys = xs;
    set ys.push(x);
    ys
}.test({
    mut a = [];
    set a.push(1);
    set a.push(2);
    set a.push(3);
    let b = a; // snapshot of [1, 2, 3]
    set a.push(4); // grows a only
    assert(a == [1, 2, 3, 4]);
    assert(b == [1, 2, 3]); // b stays [1, 2, 3]
    assert(appended(b, 4) == [1, 2, 3, 4]);
    assert(b == [1, 2, 3]);
})
--- performance ---
allocations: 10
deallocations: 10
reallocations: 0
bytes allocated: 544
instructions executed: 993
binary size: 3424
  code: 1448
    __eq_i64$arr: 296
    __test$appended: 1080
    main: 72
  data: 217
  metadata: 1759
builtin calls: 41
  aipl_arr_inc: 7
  aipl_array_dec: 17
  aipl_array_new: 5
  aipl_array_push: 5
  aipl_assert: 4
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
