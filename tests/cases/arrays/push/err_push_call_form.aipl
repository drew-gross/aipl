import { len, push } from builtins;

// `push` mutates, so the function-call form is rejected — use `xs.push(x)`.
fn main() -> i64 {
    mut xs = [1];
    let ys = push(xs, 2);
    len(ys)
}
--- errors ---
error: "push" modifies an array in place; call it as a method on a mutable array variable: "xs.push(x)"
 --> ./tests/cases/arrays/push/err_push_call_form.aipl:6:14
  |
6 |     let ys = push(xs, 2);
  |              ^^^^^^^^^^
