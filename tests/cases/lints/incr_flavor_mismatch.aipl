import { print, saturating_increment as ++, to_str, wrapping_add as + } from builtins;

// `+` and `++` here are *different* flavors — a wrapping add beside a saturating
// increment — so `set n = n + 1;` and `set n++;` genuinely differ and the lint
// stays quiet. It fires only when the file's two spellings resolve to the same
// implementation.
//
// With `incr_by_one` quiet, the compound lint is what still has advice for that
// line (`wrapping_add_assign as +=` does match this file's `+`), so it is
// squelched: the long-hand spelling is what this case is here to show.
fn main() !prints {
    mut n: u8 = 255;
    set n = n + 1; #[allow]
    // wraps to 0
    print(n.to_str());
    mut m: u8 = 255;
    set m++; // saturates at 255
    print(m.to_str());
}
--- stdout ---
0
255
--- performance ---
allocations: 2
deallocations: 2
reallocations: 0
bytes allocated: 36
instructions executed: 80
binary size: 1304
  code: 408
  data: 1
  metadata: 895
functions:
  __to_str_u8:
    calls: 2
    bytes: 204
  aipl_u64_len:
    calls: 4
    bytes: (external)
  aipl_write_u64:
    calls: 2
    bytes: (external)
  main:
    calls: 1
    bytes: 204
