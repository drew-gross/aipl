import { map, to_str, wrapping_mul as *, == } from builtins;

// `#[allow]` squelches the eta-lambda lint on its line, so this forwarding
// lambda compiles and runs as written (see err_eta_lambda.aipl for the lint).
fn stringify(xs: i64[]) -> str[] {
    xs.map(|x| to_str(x)) #[allow]
}.test({
    assert(stringify([1, 2, 3]) == ["1", "2", "3"]);
})
--- performance ---
allocations: 3
deallocations: 3
reallocations: 0
bytes allocated: 168
instructions executed: 477
binary size: 2848
  code: 1128
  data: 97
  metadata: 1623
--- monomorphizations ---
main
