import { print, saturating_decrement as --, to_str, wrapping_sub as - } from builtins;

// `-` and `--` here are *different* flavors — a wrapping subtract beside a
// saturating decrement — so `set n = n - 1;` and `set n--;` genuinely differ and
// the lint stays quiet. It fires only when the file's two spellings resolve to
// the same implementation.
//
// With the step lint quiet, the compound lint is what still has advice for that
// line (`wrapping_sub_assign as -=` does match this file's `-`), so it is
// squelched: the long-hand spelling is what this case is here to show.
fn main() !prints {
    mut n: u8 = 0;
    set n = n - 1; #[allow]
    // wraps to 255
    print(n.to_str());
    mut m: u8 = 0;
    set m--; // saturates at 0
    print(m.to_str());
}
--- stdout ---
255
0
--- performance ---
allocations: 2
deallocations: 2
reallocations: 0
bytes allocated: 36
instructions executed: 83
binary size: 1312
  code: 420
  data: 1
  metadata: 891
functions:
  __to_str_u8:
    calls: 2
    bytes: 204
  aipl_u64_len:
    calls: 4
    bytes: (external)
  aipl_write_u64:
    calls: 2
    bytes: (external)
  main:
    calls: 1
    bytes: 216
