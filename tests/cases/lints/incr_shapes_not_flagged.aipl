import { print, to_str, wrapping_add as +, wrapping_increment as ++ } from builtins;

struct Counter { n: i64 }

// The shapes the increment lint must leave alone: adding something other than
// one, adding one to a *different* binding, and a field store — which has no
// `++` spelling, since `set x++;` takes a bare binding.
//
// The first of those is still an accumulate, so the *compound* lint owns it and
// is squelched here: this case is about what `incr_by_one` does, and it can only
// show that by keeping the long-hand spelling.
fn main() !prints {
    mut n = 0;
    set n = n + 2; #[allow]
    // not one, so not the increment lint's
    mut m = 0;
    set m = n + 1; // one, but added to another binding
    mut c = Counter { n: 0 };
    set c.n = c.n + 1; // a field store, not a bare binding
    set n++; // the recommended form itself never trips its own lint
    print(n.to_str());
    print(m.to_str());
    print(c.n.to_str());
}
--- stdout ---
3
3
1
--- performance ---
allocations: 3
deallocations: 3
reallocations: 0
bytes allocated: 51
instructions executed: 132
binary size: 1448
  code: 512
  data: 1
  metadata: 935
functions:
  __to_str_i64:
    calls: 3
    bytes: 204
  aipl_i64_len:
    calls: 6
    bytes: (external)
  aipl_write_i64:
    calls: 3
    bytes: (external)
  main:
    calls: 1
    bytes: 308
