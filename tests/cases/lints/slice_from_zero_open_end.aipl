import { print, to_str } from builtins;

// The partial ranges, which no slice lint touches. Each drops exactly one bound
// already, so there is nothing left to recommend:
//
//   - `x[1..]` — an open end with a start worth keeping. (`x[0..]` *is* flagged,
//     by `slice_whole`, because a zero start makes it the whole receiver; an
//     omitted start is synthesized as a zero with an empty span, which is how a
//     written `0` is told apart from an elided one.)
//   - `x[..2]` — an open start with a real end. Only a `len` end would make this
//     the whole receiver.
fn main() !prints {
    let xs = [10, 20, 30];
    print(xs[1..].to_str());
    print(xs[..2].to_str());
}
--- stdout ---
[20, 30]
[10, 20]
--- performance ---
allocations: 5
deallocations: 5
reallocations: 0
bytes allocated: 200
instructions executed: 570
binary size: 2504
  code: 1052
  data: 67
  metadata: 1385
functions:
  __to_str_i64$arr:
    calls: 2
    bytes: 708
  aipl_arr_slice:
    calls: 2
    bytes: (external)
  aipl_array_dec:
    calls: 3
    bytes: (external)
  aipl_array_new:
    calls: 1
    bytes: (external)
  aipl_i64_len:
    calls: 8
    bytes: (external)
  aipl_write_bytes:
    calls: 6
    bytes: (external)
  aipl_write_i64:
    calls: 4
    bytes: (external)
  main:
    calls: 1
    bytes: 344
