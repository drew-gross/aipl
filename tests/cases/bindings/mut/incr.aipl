import { equal as ==, less_than as <, wrapping_increment as ++ } from builtins;

// `set n++;` increments a mut binding by one. `++` is its own operator with its
// own flavors, imported separately from `+`: `wrapping_increment as ++` here,
// `saturating_increment as ++` for the clamping one (see
// `bindings/mut/incr_saturating`). Each shares its `+` counterpart's
// implementation, since incrementing is adding 1.
fn count_to(limit: i64) -> i64 {
    mut n = 0;
    while (n < limit) {
        set n++;
    }
    n
}.test({
    assert(count_to(5) == 5);
    assert(count_to(0) == 0);
    assert(count_to(1) == 1);
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 157
binary size: 1408
  code: 268
  data: 172
  metadata: 968
functions:
  aipl_assert:
    calls: 3
    bytes: 0
  aipl_test_begin:
    calls: 1
    bytes: 0
  aipl_test_end:
    calls: 1
    bytes: 0
  aipl_test_summary:
    calls: 1
    bytes: 0
  count_to:
    calls: 3
    bytes: 68
  main:
    calls: 1
    bytes: 200
