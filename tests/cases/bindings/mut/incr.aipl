import { wrapping_increment as ++, <, == } from builtins;

// `set n++;` increments a mut binding by one. `++` is its own operator with its
// own flavors, imported separately from `+`: `wrapping_increment as ++` here,
// `saturating_increment as ++` for the clamping one (see
// `bindings/mut/incr_saturating`). Each shares its `+` counterpart's
// implementation, since incrementing is adding 1.
fn count_to(limit: i64) -> i64 {
    mut n = 0;
    while (n < limit) {
        set n++;
    }
    n
}.test({
    assert(count_to(5) == 5);
    assert(count_to(0) == 0);
    assert(count_to(1) == 1);
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 157
binary size: 1408
  code: 268
    count_to: 68
    main: 200
  data: 172
  metadata: 968
builtin calls: 6
  aipl_assert: 3
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
