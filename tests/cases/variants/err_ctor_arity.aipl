import { print, to_str } from builtins;

variant Shape = Circle(i64) | Empty

// A constructor must be applied to exactly its payload's arity.
fn main() !prints {
    print(to_str(Circle(1, 2)));
}
--- errors ---
error: constructor "Circle" expects 1 argument(s), got 2
 --> ./tests/cases/variants/err_ctor_arity.aipl:7:18
  |
7 |     print(to_str(Circle(1, 2)));
  |                  ^^^^^^^^^^^
