import { equal as ==, wrapping_sub as - } from builtins;

// A (non-recursive) variant is a legal payload of another variant: stored
// inline like a struct payload, constructed and matched structurally.
variant Inner = A(i64) | B

variant Outer = Wrap(Inner) | Empty

fn unwrap_a(o: Outer) -> i64 {
    match (o) {
        Wrap(i) => match (i) {
            A(n) => n,
            B => 0,
        },
        Empty => 0 - 1,
    }
}.test({
    assert(unwrap_a(Wrap(A(5))) == 5);
    assert(unwrap_a(Wrap(B)) == 0);
    assert(unwrap_a(Empty) == 0 - 1);
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 121
binary size: 1576
  code: 512
  data: 155
  metadata: 909
functions:
  __test$unwrap_a:
    calls: 1
    bytes: 404
  aipl_assert:
    calls: 3
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  main:
    calls: 1
    bytes: 108
