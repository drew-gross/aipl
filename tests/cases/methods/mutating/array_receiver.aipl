import { push, == } from builtins;

// A mutating method whose receiver is an array; it `push`es inside, and the
// growth is reflected in the caller's variable.
fn extend(mut self: i64[], a: i64, b: i64) {
    set self.push(a);
    set self.push(b);
}.test({
    mut xs = [1];
    set xs.extend(2, 3);
    set xs.extend(4, 5);
    assert(xs == [1, 2, 3, 4, 5]);
})
--- performance ---
allocations: 6
deallocations: 6
reallocations: 0
bytes allocated: 352
instructions executed: 434
binary size: 2616
  code: 1156
  data: 48
  metadata: 1412
builtin calls: 43
  aipl_arr_inc: 11
  aipl_array_dec: 20
  aipl_array_new: 2
  aipl_array_push: 4
  aipl_assert: 1
  aipl_inc: 2
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
--- monomorphizations ---
extend
main
