import { !, !=, == } from builtins;

// Arrays compare elementwise, in order, with length first. Nested arrays and
// str elements recurse.
fn eq_arrays(a: i64[], b: i64[]) -> bool { a == b }.test({
    assert([1, 2, 3] == [1, 2, 3]);
    assert(!([1, 2, 3] == [1, 2, 4]));
    assert(!([1, 2, 3] == [1, 2])); // length differs
    assert(!([1, 2] != [1, 2]));
    assert([] == []);

    assert(["a", "b"] == ["a", "b"]);
    assert(!(["a", "b"] == ["a", "c"]));

    assert([[1, 2], [3]] == [[1, 2], [3]]);
    assert(!([[1, 2], [3]] == [[1, 2], [4]]));
})
--- performance ---
allocations: 26
deallocations: 26
reallocations: 0
bytes allocated: 1224
instructions executed: 1772
binary size: 6416
  code: 3144
  data: 488
  metadata: 2784
builtin calls: 103
  aipl_arr_drop_arr: 4
  aipl_arr_drop_str: 4
  aipl_array_dec: 27
  aipl_array_new: 26
  aipl_assert: 9
  aipl_dec: 16
  aipl_inc: 8
  aipl_str_eq: 4
  aipl_str_len: 2
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
--- monomorphizations ---
__test$eq_arrays
main
