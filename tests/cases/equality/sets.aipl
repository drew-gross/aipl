import { !, !=, == } from builtins;

// Set equality is order-independent: same size and same members.
fn eq_set(a: #{i64}, b: #{i64}) -> bool { a == b }.test({
    assert(#{1, 2, 3} == #{3, 2, 1});
    assert(!(#{1, 2, 3} == #{1, 2})); // size differs
    assert(!(#{1, 2, 3} == #{1, 2, 4})); // member differs
    assert(!(#{1, 2} != #{2, 1}));
    assert(#{} == #{});
})
--- performance ---
allocations: 10
deallocations: 10
reallocations: 0
bytes allocated: 488
instructions executed: 574
binary size: 4784
  code: 2560
  data: 243
  metadata: 1981
--- monomorphizations ---
__test$eq_set
main
