import { wrapping_add as +, == } from builtins;

// A user function may reuse a builtin's name freely: since the builtin
// is only in scope when imported (and here it isn't), `len` resolves to
// the user's definition with no conflict.
fn len(x: i64) -> i64 { x + x }.test({
    assert(len(21) == 42);
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 19
binary size: 952
  code: 144
  data: 40
  metadata: 768
builtin calls: 5
  aipl_array_dec: 1
  aipl_assert: 1
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
--- monomorphizations ---
main
