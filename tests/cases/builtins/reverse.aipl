import { len, reverse, == } from builtins;

fn str_rev(s: str) -> str { s.reverse() }.test({
    assert(str_rev("abc") == "cba");
    assert(str_rev("") == "");
    assert(str_rev("a") == "a");
    assert(str_rev("hello world") == "dlrow olleh");
    assert(reverse("xyz") == "zyx"); // free-call form
})

fn arr_rev(xs: i64[]) -> i64[] { xs.reverse() }.test({
    assert(arr_rev([1, 2, 3]) == [3, 2, 1]);
    assert(arr_rev([42]) == [42]);
    assert(arr_rev([1, 2, 3]).len() == 3);
})

fn str_arr_rev(xs: str[]) -> str[] { xs.reverse() }.test({
    assert(str_arr_rev(["a", "b", "c"]) == ["c", "b", "a"]);
})
--- performance ---
allocations: 12
deallocations: 12
reallocations: 0
bytes allocated: 580
instructions executed: 630
binary size: 5752
  code: 2296
    __eq_0: 296
    __eq_1: 352
    main: 1648
  data: 618
  metadata: 2838
builtin calls: 113
  aipl_arr_drop_str: 2
  aipl_arr_elem_ptr: 7
  aipl_arr_reverse: 4
  aipl_array_dec: 15
  aipl_array_new: 7
  aipl_assert: 9
  aipl_dec: 32
  aipl_inc: 15
  aipl_str_eq: 8
  aipl_str_len: 2
  aipl_str_reverse: 5
  aipl_test_begin: 3
  aipl_test_end: 3
  aipl_test_summary: 1
