import { wrapping_add as + } from builtins;

pub fn add(a: i64, b: i64) -> i64 {
    a + b
}.doc({ a + b })
--- errors ---
error: `.doc` takes a string argument, not a `{ .. }` block
 --> ./tests/cases/docs/err_doc_wrong_arg.aipl:5:3
  |
5 | }.doc({ a + b })
  |   ^^^
