import { +++, == } from builtins;

// Returning a struct that owns a heap (`str`) field: the field is retained
// for the caller and released when the returned struct is dropped — no leak
// or double-free, even though the string is freshly allocated by `+`.
struct Tagged { label: str, n: i64 }

fn tag(name: str, k: i64) -> Tagged { Tagged { label: "tag: " +++ name, n: k } }.test({
    let t = tag("alpha", 7);
    assert(t.label == "tag: alpha");
    assert(t.n == 7);
})
--- performance ---
allocations: 1
deallocations: 1
reallocations: 0
bytes allocated: 40
instructions executed: 41
binary size: 1480
  code: 300
  data: 124
  metadata: 1056
builtin calls: 19
  aipl_assert: 2
  aipl_concat_lazy: 1
  aipl_dec: 6
  aipl_inc: 2
  aipl_str_eq: 1
  aipl_str_len: 4
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
--- monomorphizations ---
main
