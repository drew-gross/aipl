import { equal as ==, wrapping_add as + } from builtins;

// A function can return a struct by value. The result is materialized in a
// caller-provided slot (sret), so reading its fields afterwards is valid.
struct Point { x: i64, y: i64 }

fn make(a: i64, b: i64) -> Point { x: a, y: b }.test({
    let p = make(10, 32);
    assert(p == Point { x: 10, y: 32 });
    assert(p.x + p.y == 42);
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 66
binary size: 1320
  code: 320
  data: 100
  metadata: 900
functions:
  __eq_Point:
    calls: 1
    bytes: 104
  __test$make:
    calls: 1
    bytes: 144
  aipl_assert:
    calls: 2
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  main:
    calls: 1
    bytes: 72
