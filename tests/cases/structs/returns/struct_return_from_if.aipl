import { equal as ==, wrapping_add as + } from builtins;

// A struct returned out of an `if` — both branches build the struct, the
// merge yields the selected one, and the function returns it via sret.
struct Vec2 { x: i64, y: i64 }

fn choose(big: bool) -> Vec2 {
    if (big) { Vec2 { x: 100, y: 200 } } else { Vec2 { x: 1, y: 2 } }
}.test({
    let a = choose(true);
    let b = choose(false);
    assert(a == Vec2 { x: 100, y: 200 });
    assert(b == Vec2 { x: 1, y: 2 });
    assert(a.x + a.y + b.x + b.y == 303);
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 111
binary size: 1472
  code: 396
  data: 168
  metadata: 908
functions:
  __eq_Vec2:
    calls: 2
    bytes: 104
  aipl_assert:
    calls: 3
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  main:
    calls: 1
    bytes: 292
