import { len, push, value_or, wrapping_add as +, wrapping_mul as *, == } from builtins;

// A struct field may be an array (`str[]`), an 8-byte heap pointer. The
// field is retained on construction and released when the struct drops.
struct Tags { id: i64, names: str[] }

fn make() -> Tags {
    mut ns = ["a"];
    set ns.push("b");
    Tags { id: 7, names: ns }
}.test({
    let t = make();
    assert(t.names[0].value_or("?") == "a");
    assert(t.names[1].value_or("?") == "b");
    let n: i64 = len(t.names);
    assert(t.id * 10 + n == 72);
})
--- performance ---
allocations: 2
deallocations: 2
reallocations: 0
bytes allocated: 88
instructions executed: 199
binary size: 2984
  code: 1116
    main: 1116
  data: 175
  metadata: 1693
builtin calls: 47
  aipl_arr_drop_str: 2
  aipl_arr_inc: 6
  aipl_arr_retain_ptr: 2
  aipl_array_dec: 8
  aipl_array_new: 1
  aipl_array_push: 1
  aipl_assert: 3
  aipl_dec: 11
  aipl_inc: 8
  aipl_str_eq: 2
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
