struct Point { x: i64, y: i64 }

fn main() {
    let p = Point { x: 1, y: 2 };
    set p.x = 3;
}
--- errors ---
error: set: cannot assign to immutable binding "p" (use "let mut")
 --> ./tests/cases/structs/fields/err_set_field_immutable.aipl:5:9
  |
5 |     set p.x = 3;
  |         ^^^^^^^
