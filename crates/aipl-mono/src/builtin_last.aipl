import { equal as ==, len, wrapping_sub as - } from builtins;

// The AIPL implementation of the `last` builtin (see `AIPL_BUILTIN_SOURCES` in
// `lib.rs`); `first`'s mirror.
//
// On an empty array `len() - 1` wraps, and an out-of-range index is `none` —
// the same answer the empty case wants, by the ordinary index rule rather than
// a special case.
# The last element, or `none` if the array is empty.
pub fn last<T: any>(self: T[]) -> T? { self[self.len() - 1] }.test({
    assert([1, 2, 3].last() == some(3));
    assert([7].last() == some(7));
    assert(last(["a", "b"]) == some("b")); // free-call form
    // Empty: `len() - 1` wraps, and an out-of-range index is `none`.
    assert([1, 2][..0].last() == none);
})
--- performance ---
allocations: 5
deallocations: 5
reallocations: 0
bytes allocated: 256
instructions executed: 358
binary size: 3616
  code: 1788
  data: 213
  metadata: 1615
functions:
  __eq_i64$opt:
    calls: 1
    bytes: 100
  __test$last:
    calls: 1
    bytes: 1588
  aipl_arr_slice:
    calls: 1
    bytes: (external)
  aipl_array_dec:
    calls: 5
    bytes: (external)
  aipl_array_new:
    calls: 4
    bytes: (external)
  aipl_assert:
    calls: 4
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  main:
    calls: 1
    bytes: 100
