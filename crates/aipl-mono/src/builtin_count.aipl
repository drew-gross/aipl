import { equal as ==, wrapping_increment as ++ } from builtins;

// The AIPL implementation of the `count` builtin. Monomorphization registers
// this function under the canonical name `__builtin_count` (see
// `AIPL_BUILTIN_SOURCES` in `lib.rs`), so a user's `xs.count(x)` resolves
// through the ordinary generic machinery — the in-file name `count` is only for
// this file's own tests, which read exactly like user code.
//
// The counterpart of `count_while`, which counts a *leading run* matching a
// predicate; this counts *every* element equal to a value, anywhere. Unsigned,
// like `len`: a count is never negative.
pub fn count<T: any>(self: T[], x: T) -> u64 {
    mut n: u64 = 0;
    for (let e : self) {
        if (e == x) {
            set n++;
        };
    }
    n
}.doc("""
    Counts how many elements of `self` equal `x`, comparing with `==` (so strings
    and chars compare by value). Generic over the element type and callable as a
    method: `xs.count(x)`. A `str` receiver counts bytes, since `str` and `char[]`
    share a representation: `s.count('\\n')` is the number of newlines in `s`.

    See `count_while` for the leading-run-matching-a-predicate form.
""").test({
    assert([1, 2, 3, 2].count(2) == 2);
    assert([1, 2, 3].count(9) == 0); // absent
    assert([].count(1) == 0); // empty
    assert([7, 7, 7].count(7) == 3); // every element
    assert(count([2, 2], 2) == 2); // function-call form too

    // Generic over the element type — the same definition serves char and str,
    // and comparison is by value, not identity.
    assert(['a', 'b', 'a'].count('a') == 2);
    assert(["go", "stop", "go"].count("go") == 2);

    // A `str` receiver counts bytes: `str` and `char[]` share a representation.
    assert("a\nb\nc".count('\n') == 2);
    assert("abc".count('\n') == 0);
    assert("".count('\n') == 0);
})
--- performance ---
allocations: 7
deallocations: 7
reallocations: 0
bytes allocated: 332
instructions executed: 1393
binary size: 4944
  code: 2128
  data: 552
  metadata: 2264
functions:
  __test$count:
    calls: 1
    bytes: 1600
  aipl_arr_drop_str:
    calls: 1
    bytes: (external)
  aipl_array_dec:
    calls: 6
    bytes: (external)
  aipl_array_new:
    calls: 6
    bytes: (external)
  aipl_assert:
    calls: 10
    bytes: (external)
  aipl_dec:
    calls: 7
    bytes: (external)
  aipl_inc:
    calls: 3
    bytes: (external)
  aipl_str_alloc:
    calls: 1
    bytes: (external)
  aipl_str_data:
    calls: 3
    bytes: (external)
  aipl_str_eq:
    calls: 3
    bytes: (external)
  aipl_str_iter_init:
    calls: 4
    bytes: (external)
  aipl_str_iter_next:
    calls: 3
    bytes: (external)
  aipl_str_len:
    calls: 9
    bytes: (external)
  aipl_test_begin:
    calls: 1
    bytes: (external)
  aipl_test_end:
    calls: 1
    bytes: (external)
  aipl_test_summary:
    calls: 1
    bytes: (external)
  count$char$s0:
    calls: 3
    bytes: 232
  count$i64:
    calls: 4
    bytes: 224
  main:
    calls: 1
    bytes: 72
