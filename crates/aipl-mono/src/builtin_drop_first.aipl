import { drop_n, equal as ==, first, len } from builtins;

// The AIPL implementation of the `drop_first` builtin (see
// `AIPL_BUILTIN_SOURCES` in `lib.rs`) — `drop_n(1)`, named for the common case.
pub fn drop_first<T: any>(self: T[]) -> T[] { self.drop_n(1) }.doc("""
    Every element except the first. An empty array stays empty.
""").test({
    assert([1, 2, 3].drop_first().len() == 2);
    assert([1, 2, 3].drop_first().first() == some(2));
    assert([7].drop_first().len() == 0);
    assert([1, 2][..0].drop_first().len() == 0); // already empty
    assert(drop_first(["a", "b"]).first() == some("b")); // free-call form
})
--- performance ---
allocations: 11
deallocations: 11
reallocations: 0
bytes allocated: 480
instructions executed: 335
binary size: 3984
  code: 1640
    __eq_i64$opt: 100
    __eq_str$opt: 148
    drop_first$i64: 52
    main: 1340
  data: 358
  metadata: 1986
builtin calls: 46
  aipl_arr_drop_str: 2
  aipl_arr_retain_ptr: 1
  aipl_arr_slice: 6
  aipl_array_dec: 11
  aipl_array_new: 5
  aipl_assert: 5
  aipl_dec: 7
  aipl_inc: 5
  aipl_str_eq: 1
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
