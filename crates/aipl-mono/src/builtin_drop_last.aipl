import { drop_last_n, equal as ==, last, len } from builtins;

// The AIPL implementation of the `drop_last` builtin (see
// `AIPL_BUILTIN_SOURCES` in `lib.rs`) — `drop_last_n(1)`, named for the common
// case.
pub fn drop_last<T: any>(self: T[]) -> T[] { self.drop_last_n(1) }.doc("""
    Every element except the last. An empty array stays empty.
""").test({
    assert([1, 2, 3].drop_last().len() == 2);
    assert([1, 2, 3].drop_last().last() == some(2));
    assert([7].drop_last().len() == 0);
    assert([1, 2][..0].drop_last().len() == 0); // already empty
    assert(drop_last(["a", "b"]).last() == some("a")); // free-call form
})
--- performance ---
allocations: 11
deallocations: 11
reallocations: 0
bytes allocated: 480
instructions executed: 405
binary size: 4504
  code: 1948
    __eq_i64$opt: 100
    __eq_str$opt: 148
    drop_last$i64: 192
    main: 1508
  data: 356
  metadata: 2200
builtin calls: 56
  aipl_arr_drop_str: 2
  aipl_arr_inc: 5
  aipl_arr_retain_ptr: 1
  aipl_arr_slice: 6
  aipl_array_dec: 16
  aipl_array_new: 5
  aipl_assert: 5
  aipl_dec: 7
  aipl_inc: 5
  aipl_str_eq: 1
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
