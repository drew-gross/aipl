[package]
name = "aipl-parser"
version = "0.1.0"
edition = "2021"
description = "The AIPL parser's Rust-side entry points and post-parse rewrites"
license = "MIT"

[dependencies]
aipl-syntax = { workspace = true }

[lib]
path = "src/lib.rs"

[lints]
workspace = true
