[package]
name = "aipl-parser"
version = "0.1.0"
edition = "2021"
description = "AIPL lexer and gazelle-based parser"
license = "MIT"

[dependencies]
aipl-syntax = { workspace = true }
gazelle-parser = { workspace = true }
gazelle-macros = { workspace = true }

[lib]
path = "src/lib.rs"
