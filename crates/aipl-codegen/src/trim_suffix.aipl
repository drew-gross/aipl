// Used by the raw-string processor to drop the newline (and CR) that end a blank
// closing line.
import { len, wrapping_sub as -, == } from builtins;

pub fn trim_suffix(self: str, c: char) -> str {
    if (self[self.len() - 1] == some(c)) { self[..self.len() - 1] } else { self }
}.doc("Drops a single trailing `c` if present, else returns `self` unchanged.").test({
    assert("rest\n".trim_suffix('\n') == "rest"); // trailing char dropped
    assert("rest".trim_suffix('\n') == "rest"); // absent -> unchanged
    assert("".trim_suffix('\n') == ""); // empty source (index -1 is none)
    assert("yxx".trim_suffix('x') == "yx"); // only the last is dropped
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 237
binary size: 2776
  code: 892
  data: 274
  metadata: 1610
builtin calls: 54
  aipl_array_dec: 1
  aipl_assert: 4
  aipl_char_at: 4
  aipl_dec: 20
  aipl_inc: 10
  aipl_str_eq: 4
  aipl_str_len: 6
  aipl_str_slice: 2
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
--- monomorphizations ---
main
trim_suffix
