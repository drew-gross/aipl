// String-only; walks once, tracking the position just past the last
// *non*-matching char.
import { wrapping_increment as ++, !, == } from builtins;

pub fn trim_end_while(self: str, pred: (char) -> bool) -> str {
    mut k = 0;
    mut i = 0;
    for (let c : self) {
        set i++;
        if (!pred(c)) {
            set k = i;
        }; // k = end of the last non-matching run
    }
    self[..k]
}.doc("Drops the trailing run of characters satisfying `pred` (the trailing counterpart of `trim_while`).").test({
    assert("abc   ".trim_end_while(|c| c == ' ') == "abc"); // trailing spaces dropped
    assert("abc".trim_end_while(|c| c == ' ') == "abc"); // nothing to trim
    assert("   ".trim_end_while(|c| c == ' ') == ""); // all trimmed
    assert("".trim_end_while(|c| c == ' ') == ""); // empty source
    assert("axxbxx".trim_end_while(|c| c == 'x') == "axxb"); // only the trailing run
})
--- performance ---
allocations: 0
deallocations: 0
reallocations: 0
bytes allocated: 0
instructions executed: 964
binary size: 3936
  code: 1764
  data: 395
  metadata: 1777
builtin calls: 61
  aipl_array_dec: 1
  aipl_assert: 5
  aipl_dec: 15
  aipl_inc: 5
  aipl_str_data: 4
  aipl_str_eq: 5
  aipl_str_iter_init: 5
  aipl_str_iter_next: 4
  aipl_str_len: 9
  aipl_str_slice: 5
  aipl_test_begin: 1
  aipl_test_end: 1
  aipl_test_summary: 1
--- monomorphizations ---
main
